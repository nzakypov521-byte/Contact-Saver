export interface Contact {
    id: string,
    name: string,
    surname: string,
    phone: string,
    image?: string
}

export interface InpDataType {
    id?: string,
    name: string,
    surname: string,
    phone: string
}